'use client'

import Image from "next/image";

const Navvy = () => {

    return (
        <div className="">
            <div>
                <Image src="images/logo-turnoffsolana.svg" alt="logo" width={300} height={100} />
            </div>
        </div>
    );
};

export default Navvy;